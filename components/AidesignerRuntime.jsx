'use client';

/**
 * Noise Shimmer runtime intentionally disabled — third-party hero effects
 * read as tech-demo, not quiet clinical retail. Keep the module so imports
 * don't break if restored later behind a flag.
 */
export default function AidesignerRuntime() {
  return null;
}
